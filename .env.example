# Mistral AI: https://console.mistral.ai/
MISTRAL_API_KEY=GCGRxxldb2ciEk8tlDcTrBj2awsfXF1g

# Optional: model (default: mistral-small-latest)
# MISTRAL_MODEL=mistral-small-latest

# Agent: max LangGraph steps (model + tool turns). Lower = fewer API calls.
# AGENT_RECURSION_LIMIT=12

# Retries when API returns HTTP 429 (exponential backoff)
# LLM_INVOKE_RETRIES=5
# LLM_RETRY_BASE_SEC=3.0

# Mistral client retries (keep low; invoke-level retries also apply)
# MISTRAL_SDK_MAX_RETRIES=2
